import React from "react";

const DirectoryIcon = (props) => {
    return <div className="btn-icon btn-pill btn btn-outline-primary directory-btn">{props.name}</div>;
};

export default DirectoryIcon;
